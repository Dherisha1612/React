// Task 27: Find First Even Number
// Problem Statement: Write a function that takes an array of numbers and returns the first even number in the array using the find method.
// let array=[1, 3, 7, 10, 2]
// const even=array.find((num)=>{
//     return num %2==0
//  })
// console.log(even)
// Write a function that takes an array of student objects and a name, and returns the student object that matches the given name using the find method. Each student object has properties name and age.

// Write a function that takes an array of objects representing items in a shopping cart and returns the total price using the reduce method. Each object contains a price property.
const cart = [
  { item: 'apple', price: 1.5 },
  { item: 'banana', price: 2.0 },
  { item: 'orange', price: 1.25 }
]

function calculateTotal(){
    b=[]
    for(i=0;i<cart.length;i++){
        if(cart[i].price)
            b.push(a)
        }
    console.log(b)
}
